---
- name: terraform
  hosts: 127.0.0.1
  tasks:
    - terraform:
        project_path: "/var/lib/awx/projects/_8__second/yandex"
        state: present
        lock: false
      register: loginoutput

    - set_fact:
        cp_gate_ip: "{{ loginoutput.outputs.internal_ip_address_cp_gw.value }}"
        cp_gate_name: "{{ loginoutput.outputs.name_cp_gw.value }}"

    - debug:
        msg: "{{ cp_gate_name }}"
